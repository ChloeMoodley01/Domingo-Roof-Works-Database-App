@model IEnumerable<DomingoApp.Models.JobInformation>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*  (W3schools, 2021) and //(4. Continuation and close off, 2021) *@
@**This is goting to display all the customer information in a table on the index page*@
<html>
<head>
    <style>
        table, th, td {
            border: 1px solid black;
        }

        th, td {
            padding: 10px;
        }
    </style>
</head>


<body>
    <h1 style="text-shadow: 8px 8px 5px #FA8072; font-family:Arial">Job Index</h1>
    <p>In the table below you can view all the job card numbers that are in the database. You will be able to view, update or delete the number of days the work will be done and the job card number.
    <p><a asp-action="Create" class="btn btn-success"> Add new Job </a></p>

    <table style="width:50%">
        <tr>
            <th>Job Card No.</th>
            <th>Number of Days</th>
            <th>Details/Update/Delete</th>
        </tr>

        @foreach (var jobInfo in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => jobInfo.JobCardNo)</td>
                <td>@Html.DisplayFor(modelItem => jobInfo.NumOfDays)</td>
                <td>
                    <a asp-action="Details" asp-route-id="@jobInfo.JobCardNo" class="btn btn-light">Details</a>
                    <a asp-action="Update" asp-route-id="@jobInfo.JobCardNo" class="btn btn-info">Update</a>
                    <a asp-action="Delete" asp-route-id="@jobInfo.JobCardNo" class="btn btn-primary">Delete</a>
                </td>
            </tr>
        }
    </table>

    <style>
        .butt {
            color: #FA8072;
        }
    </style>

    <div>
        <a class="butt" asp-area="" asp-controller="Home" asp-action="Index">Back to Home Page</a>
    </div>

</body>
</html>
